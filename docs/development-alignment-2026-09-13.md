# 开发对齐记录 · 2026-09-13

## 整合结果

- 原远端主线：`70ee0e7`。
- 本地 `v2`：`44c9659`（ASC 指引）与 `045b72f`（新版 Mika、iPad 单栏）已进入 `main`。
- [PR #13](https://github.com/Yidada/chatty/pull/13) / `df884a1`：回车发送整合进新版输入区。解决 ChatScreen、宿主测试与生成工程冲突，保留历史抽屉、过程折叠、停止/队列、八行高度、24pt 圆角与构建号 11。
- Xcode 工程通过现有生成器重建，未手工保留冲突后的项目引用。

## 验证

| 检查 | 结果 |
| --- | --- |
| ChattyKit `swift test` | 98 项，0 失败；本次输入区合并未改变共享核心 |
| ChattyFixture 宿主 XCTest | 14 项，0 失败，1 项既有模拟器文件保护跳过 |
| Chatty iOS Simulator 构建 | BUILD SUCCEEDED |
| 键盘回车发送 | iPhone Air / iOS 26.5 合成服务实测，键盘显示 send，消息进入服务端且无换行 |
| 空草稿与重复回车 | 服务端审计两条独立测试消息各投递一次；空草稿没有增加计数 |
| 新版界面保留 | 实测发送后出现停止生成，完成后显示回复；停止与队列代码保持原实现 |
| Git | 冲突标记和 diff whitespace 检查通过 |

原始日志、合成服务审计与旧工作目录保存在仓库外的本次备份目录。此前 PR 的截图与 75/11 项测试记录属于旧基线，最新整合结果以上表为准。

本次未重新发布 TestFlight、Android、Mac 或 Web。构建号 11 的历史发布记录不包含本次刚整合的回车发送。中文输入法手动体验、硬件键盘快捷键和完整停止/队列回放没有在本次重跑；输入法 composition 的隔离由真实 UITextView 宿主测试覆盖。

## 清理依据

- 本地 8 个旧分支移除，其中 Android / Apple activity 分支已通过 patch-equivalence 确认在主线存在；其他分支均已被主线包含。
- 5 个旧 worktree 整体移到仓库外，保留原构建、归档、安装包和本地证据；解除 Git worktree 注册。
- 旧 stash 已有完整 Git bundle 和补丁备份。代码已被更新后的主线覆盖；缺少的 5 个文件是历史 Web 原始截图/日志，留在备份中。
- PR #3 的 V3 intent 已由 #4 覆盖，关闭重复入口；CLE-70 / CLE-95 继续保留。
- PR #6 的批量功能已由 `8fd4bf9` 重新集成并经 #12 合入，关闭旧构建准备分支。
- 已合并分支与已关闭的旧计划分支清理；所有清理前 refs 已保存在 Git bundle 中。
- [PR #7](https://github.com/Yidada/chatty/pull/7) / `agent/mika/01a08d0a` 仍有独立插桩成果，保留为唯一未合并分支。本地与远端该分支一致；与 `main` 的功能差异有意保留。
- README 改为当前实现入口，移除早期 Lark Context Layer 产品设想；历史可从 Git 查阅。

## Multica 对齐口径

- CLE-101：代码进入主线且上述测试通过。人工输入法/硬件键盘验收尚未补齐，仍处于 in_review。
- CLE-99：当前代码有动态列表、独立分页与已读指纹，但没有多选/批量或滑动动作，修正为 backlog 并保留原验收范围。
- CLE-70 / CLE-95：B0 和数值预算尚未完成。PR #7 只证明插桩代码存在。
- CLE-64：语音转写仍为待办。
- 已取消的历史任务保留取消状态，已完成的历史任务保留证据；当前项目说明只列有效基线和待办。
