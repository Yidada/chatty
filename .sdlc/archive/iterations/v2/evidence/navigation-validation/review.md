# 导航连续性自审

- 审查者：当前实现者；非独立审查。
- 对照范围：Tab 状态、原生完整详情、可取消工作区切换；未增加其他 Agent 对话、网页出口或配置编辑。
- Shell 仅按凭据和工作区重建，Tab 自身持有状态所有者；只渲染当前 Tab，BackHandler 与生命周期加载按可见性启停。
- 项目刷新保留已加载范围，通过 generation 拒收旧列表响应；离开详情同样拒收迟到详情响应。状态写入单飞、revision 和 suppress_run 契约保持。
- 工作区面板开启与关闭不清空 selected；同工作区不重新写入选择；实际切换后旧作用域取消。
- 41 项 JVM 测试与 Lint 通过。Pixel 连续性用例和项目管理回归通过。
- Issue 55 在切换前后的位置均为 [140,2461][353,2527]，没有退回第一页。聊天历史消息锚点保持。
- 已人工查看完整 Issue / Runtime 详情、工作区面板、深色 Issue 和大字号面板截图，标题、正文、关闭/返回操作可见，无遮挡。
- 应用冷启动仍重取服务端实体；当前运行中的完整导航位置未承诺跨进程恢复。
- 原有聊天回归通过：两轮合成收发、任务过程、重连、草稿冷启动、最早消息分页、无回复、失败详情和 503 恢复。
- 最终真实包导航检查通过，APK 哈希记录在 apk.json；当前进程无 AndroidRuntime FATAL。测试包与端口已清理，手机保留真实服务版本。
