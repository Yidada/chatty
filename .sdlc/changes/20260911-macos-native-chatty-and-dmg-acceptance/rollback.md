# 回退

独立 ai.chatty.macos 应用不替换 iOS/Android。出现数据隔离、发送重复或严重崩溃时关闭 Mac 客户端，停止继续发送；回退到前一验收 DMG（首版则停用 Mac 端并继续移动端）。应用替换前退出进程；默认保留本地草稿，清理当前客户端数据须通过退出登录或明确的用户决定。已经提交到后端的消息与任务无法靠卸载撤销，需要逐项核对，禁止自动重发。开发回退只撤回此变更的 macos/ 和本轮共享核心差异，不 reset 工作区或删除其他人的变更。回退后验证移动端仍可用、Mac 无残留网络活动、草稿恢复行为明确。
