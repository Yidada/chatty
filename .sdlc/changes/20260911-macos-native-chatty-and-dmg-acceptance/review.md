# 实施评审

- Verdict: approved
- 评审方式：实施代理自审；没有独立评审者。本结论针对已批准的本地 DMG 交付。
- 已检查单窗口生命周期、共享模型保持、项目快照与 FIFO 复用、桌面剪贴板/QuickLook 适配、独立凭据与存储命名、最小沙盒权限、生产/Fixture 分离、签名和打包脚本。
- 共享核心修改局限于 macOS 私有文件权限，新增测试验证原子替换仍保持权限；iOS 编译回归通过。
- 6 次合成发送审计与桌面观察一致，待处理已读未触发任务写入。
- 导出的公证应用经过 DMG 挂载复制、Gatekeeper 和启动验证，交付内容与证据一致。
- 未发现阻断本地验收的问题。非阻断构建警告、Intel 实机与真实账号等未覆盖项已列入 evidence.md 和 README，后续按用户反馈处理。
- 原有 Android/iOS 工作未纳入本轮交付修改；不自动提交、合并或推送。

## 提交前隐私检查

用户随后授权单独提交本变更。签名身份与 Team ID 改为必填环境变量，审批人匿名化，公证引用和本机路径脱敏；原始日志、DMG、证书和本地工具目录不纳入提交。使用匿名 Git 作者与提交者，不修改全局 Git 配置。

隐私脱敏将 risk.md 负责人改为匿名角色。已关闭变更的 helper 拒绝重新记录门禁，因此仅更新脱敏内容指纹，并在 state.json 保留 pre_redaction_fingerprint；原批准时间及范围保持不变。图标仅删除 EXIF/文本元数据，像素数据保持不变。
