# 回滚方案

当前只补充计划记录，无产品或存储变更。

后续最小修复须独立提交，记录旧版 APK、提交 SHA 和验证命令；出现权限、缓存隔离或发送回归时停止实验并回退该提交。
缓存格式设计须保证旧版可忽略新缓存；回滚仅清理新业务缓存，不清凭据和草稿，不通过卸载普通包恢复。
回退后验证登录、账号/工作区隔离、消息读取、草稿和发送回执处理。已发生的真实外部写入无法靠代码回滚撤销，因此当前实验只使用 synthetic 数据。
